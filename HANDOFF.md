# HANDOFF.md

Living state doc — current as of 2026-08-09. Update this when picking work
back up or handing off; it's expected to go stale and get rewritten,
unlike CLAUDE.md. Written assuming the reader (human or agent) knows
nothing beyond this file and CLAUDE.md — if something here references
work you can't find, say so rather than assuming it exists.

## Phase status

- **Phase 0** — done. Schema + migrations + `AMMN` end-to-end.
- **Phase 1a** — done. 962 active tickers, full Yahoo backfill.
- **Phase 1b** — done, within its documented bound. 27 delisted tickers
  recovered; pre-2020-01-02 delisted tail is a permanent, open gap (IDX's
  own `GetStockSummary` endpoint has no data before that date — verified
  empirically, not a bug).
- **Phase 2** — complete (schema fix + Parts A-G) but **deliberately left
  OPEN**. Spec §7's "10 consecutive green runs" acceptance criterion
  cannot meaningfully accrue on a laptop that sleeps, loses power, or is
  simply off overnight. Closes when this runs on real infrastructure —
  see remaining thread 6 below. A large follow-on investigation (below)
  came out of building Phase 2's cross-source reconciliation check and is
  not yet fully closed out either.
- **Phase 3+** — not started. See remaining thread 5 before starting it.

## IMMEDIATE NEXT ACTION

**Tier-by-tier review of `review/corporate_actions_candidates.md` with
Caroline.** This file was generated by `jobs/generate_ca_review.py` and
proposes rows for the (currently empty) `corporate_actions` table.
**Nothing in it has been applied to the database.** Structure:

- **Tier 1 (76 rows, confidence 4-5):** clean-fraction splits (ratio is a
  clean reciprocal like 0.2=1:5, 0.5=1:2, 0.125=1:8), permanent, real
  matching volume on both sources at the transition. Sorted with
  known-checkable tickers first — `BBNI`, `AKRA`, `BBCA` landed at the
  top with ratios and dates matching real splits Caroline already knows.
  **Review this tier first.**
- **Tier 2 (110 rows, confidence 2-3):** new candidates, not individually
  verified. `action_type` is a best-effort label
  (`bonus_share_or_rights` for odd sub-1.0 ratios, `unknown` otherwise) —
  never guessed past what the ratio actually supports.
- **Tier 3 (10 rows):** self-reverting divergences. **Listed as open
  questions only — NOT proposals.** A divergence that undoes itself is
  not a corporate action by definition; do not seed `corporate_actions`
  from this section under any circumstance.
- **Excluded (34 rows):** transitions that matched the detection shape
  but failed a plausibility check, with the specific reason logged per
  row (see "Gotchas" below for the two concrete cases that drove this
  guard). Not proposals, not open questions — data artifacts.

**Nothing gets committed to `corporate_actions` without Caroline's
explicit sign-off, tier by tier.** Do not batch-approve. Do not skip to
Tier 2 because Tier 1 looks solid.

## THE BIG FINDING — Yahoo's `close_raw` is not reliably raw

This is the reason Phase 2 grew a large sub-investigation before closing.
It started as a single-ticker question (why do `FASW`'s Yahoo and IDX
prices disagree?) and turned into a systemic finding affecting hundreds
of tickers. Three distinct, independently-verified failure modes, not one:

1. **Yahoo retroactively applies stock splits even with
   `auto_adjust=False`.** This is a real, if under-documented, Yahoo
   behavior: their "unadjusted" `Close` excludes dividends but NOT
   splits — the entire historical series gets rescaled the moment a
   split happens, silently. ~106 tickers affected in our data (Tier 1 of
   the review file). Verified concretely via `PTRO`: IDX's raw feed shows
   close jumping 27,450 → 2,880 in one day (2025-01-03, -89.5%) with
   volume exploding 17x (7.5M → 126.5M shares) — the unambiguous
   signature of a real 1-for-10 split. Yahoo's close was already on the
   post-split scale for the *entire* prior history.
2. **Yahoo has had at least 3 multi-day, market-wide outages since 2020**
   (2022-03-11/14, 2024-01-12/15, 2024-03-21/26 — 85 to 140 tickers each),
   **unrelated to any real IDX event.** Proven from our own data, not
   inferred: IDX's market breadth (tickers with non-zero volume, total
   value traded, vs. trailing 20-day median) is completely normal
   (94-109% of baseline) during all three windows. The positive control —
   the real March 2020 COVID-crash trading halt — shows a genuine 155%
   value-traded spike on the actual halt day. Same detection signature
   (many tickers, Yahoo volume=0, IDX moving normally) but only one of
   the four has a real cause behind it.
3. **Yahoo silently freezes individual tickers for extended runs, with no
   other visible defect.** `BNBR`: Yahoo shows a flat 3800.0 for 767
   days (2020-01-02 → 2023-02-15) while IDX shows continuous real trading
   underneath (738/738 days with non-zero volume, actual price range
   50-91 — Yahoo's frozen value is 42-76x the real price, not just stale,
   wrong). `ADMF`: flat 289 days, same shape. The run-length distribution
   across all affected tickers is bottom-loaded — median 3 days, 90 of
   106 isolated-freeze runs are 2-5 days. Long freezes like BNBR/ADMF are
   the dramatic examples; short ones are the common case and the easiest
   to miss, which is exactly why `jobs/validate.py::check_frozen_price_divergence`
   (the `full-history-audit` command) exists now as a standing check.

## CONSEQUENCE (decision, not a note)

**`close_adj` and return-series work are fine as-is.** The split-retroactive-
adjustment behavior that broke `close_raw` is *exactly* what makes
`close_adj` usable — Yahoo doing that adjustment is the point of that
column. **`close_raw` from Yahoo is unreliable for any level-based work**:
tick size, ARA/ARB band checks, gap detection, price-based liquidity
filters. Per CLAUDE.md's new SOURCE AUTHORITY convention: IDX is
authoritative for `close_raw` (2020-01-02 onward), Yahoo is authoritative
for `close_adj`. **Phase 4's feature builder must be explicit about which
column, and which source, each feature reads** — this is not optional
follow-up, it's a design constraint the feature builder has to satisfy
from its first line of code.

**Pre-2020 Yahoo history: downgraded from "exploration-only" to NOT USABLE
for quantitative work.** Previously this repo's stance was "kept locally
for research, excluded from production scope." That's no longer strong
enough — no second source exists before 2020-01-02 (IDX's own retention
wall, see Phase 1b above), so split contamination and silent freezes in
that period are **both undetectable and unfixable**. There is no way to
know whether any given pre-2020 Yahoo value is trustworthy. Treat it as
opt-in-with-explicit-acknowledgment-of-risk, not opt-out.

## What's built

Everything in CLAUDE.md's layout section exists and is wired together:
seed → backfill → historical harvest → daily incremental → validate →
reconcile → classify → generate review → alert, plus backup/restore/verify
and (unloaded) launchd scheduling. `known_issues` holds 49 rows (47
tickers + 2 date-ranges) seeded from confirmed Phase 1 findings — note
this predates and is separate from the corporate-actions investigation
above; nothing from that investigation has been added to `known_issues`.

## What's deliberately NOT built

- **Telegram (or any non-console) notifier.** `Notifier` ABC exists,
  `ConsoleNotifier` is the only implementation. Adding one is a new class
  + registering it in `notify.py::get_notifier()`'s backend switch —
  intentionally left as the trivial case it was designed to be, not
  started because there's no channel to send to yet.
- **launchd plists are not loaded.** Written, `plutil`-validated, wrapper
  scripts smoke-tested directly — but not copied into
  `~/Library/LaunchAgents/` or `launchctl load`ed. That starts an
  indefinite recurring automation hitting live external services; left
  for Caroline to load when ready, not started silently.
- **`broker_flow_daily` is schema-only, zero rows.** Phase 3 hasn't
  started. See remaining thread 5 — the sizing estimate currently on
  record (24M rows/yr) is a flagged worst case, not a number to build on.
- **Suspension-vs-defect distinction (`ticker_status_daily`).** Designed
  in conversation, not implemented — see remaining thread 3.
- **`corporate_actions` backfill from the review file.** Generated,
  awaiting tier-by-tier human sign-off — see "Immediate next action" above.
  Not automated, not scheduled to become automated.

## Remaining threads, in order

1. **Tier-by-tier `corporate_actions` review** — see "Immediate next
   action" above. Blocks nothing else, but is the explicit next step.
2. **Step 3 source-of-truth policy implementation.** The *decision* is
   made (see CONSEQUENCE above, and CLAUDE.md's SOURCE AUTHORITY
   convention) — IDX authoritative for `close_raw` 2020+, Yahoo
   authoritative for `close_adj`. What's not yet done: encoding this
   anywhere code actually enforces it (today it's a documented
   convention, not a guardrail — nothing stops a future query from
   reading Yahoo's `close_raw` for level-based work by accident).
3. **Zero-volume check misclassifies suspensions.** `ASMI`, `BKDP`,
   `COAL`, `LCKM`, `RGAS`, `MDIA`: consecutive zero-volume days while
   still ranking top-300 by trailing median volume — almost certainly
   trading suspension or full call auction (Papan Pemantauan Khusus), not
   bad data. `jobs/validate.py::check_zero_volume_top300` reports it as a
   failure when it's real market information, and the finding self-heals
   (silently disappears) in ~2 weeks as the zeros drag the trailing
   median down below the top-300 cutoff — nobody learns what happened.
   **`known_issues` is the wrong tool** (permanent-suppression would hide
   a real re-listing — suspensions end). Design direction, not yet built:
   an explicit `ticker_status_daily` concept (trading / suspended /
   call_auction), derived initially from a heuristic (≥3 consecutive
   zero-volume days), but check first whether IDX exposes real suspension
   announcements to ingest as ground truth instead of inferring it — if
   so, that's strictly better than the heuristic. Matters for Phase 4
   too: suspended bars need to be excludable from feature building, and
   "currently suspended" is itself a feature.
4. **Alert / log noise.** Current output would be unreadable in Telegram.
   Collapse the 47(+)-row `insufficient_yahoo_history` suppressions to
   one summary line; alerts should carry counts + at most 3 examples per
   check with a pointer to the full report, not the full finding list
   inline; silence yfinance's own "possibly delisted" stderr chatter for
   tickers already known-suspended (`jobs/validate.py` already logs a
   structured warning for the same fact right after — pure duplication).
5. **Phase 3 gate — measure before designing.** Do not start designing
   `broker_flow_daily` ingestion, ranking, or anything else until this
   runs: fetch **one real day** of IDX broker summary and measure the
   actual distinct-broker-codes-per-ticker distribution
   (min/median/p90/max, and how many tickers appear at all). The number
   currently on record — 24M rows/yr, from 960 tickers × 100 broker codes
   × 250 days — is an explicitly flagged worst case; only ~650 tickers
   trade on a typical day (measured) and broker-code counts per ticker
   are believed heavily skewed, so a realistic figure is probably nearer
   4M rows/yr, roughly 6x lower. **Thread 6 depends on this being
   measured, not assumed.** Also already decided, not to be
   re-litigated when Phase 3 starts: `broker_flow_daily` scopes to a
   liquidity-filtered top-~250-by-median-value-traded subset, using the
   same query shape `jobs/validate.py::check_zero_volume_top300` already
   implements (there, ranked by median volume; Phase 3's filter ranks by
   median value traded — spec §6's own modeling guardrail, not a new idea).
6. **Railway vs. managed Postgres — deliberately deferred, blocked on
   thread 5.** Local Docker Postgres remains the target for everything
   until a real sizing number exists for `broker_flow_daily` — deciding
   hosting on the current worst-case estimate risks either
   over-provisioning now or under-provisioning the moment Phase 3 lands.
   Everything routes through `DATABASE_URL`, so this should be a config
   change when it happens, not a code change — verify that's still true
   before deciding.

## Decisions and why (the part compaction destroys)

- **`prices_daily`'s PK was widened to include `ingested_at`, rejecting a
  companion audit-table design.** The audit-table alternative's failure
  mode is *silent*: a reader that forgets to merge the side table gets
  back one plausible-looking row containing the future-corrected value,
  no error, no duplicate, no signal anything's wrong. The widened-PK
  approach's failure mode is *loud*: get the write path wrong and you get
  duplicate rows per (ticker, date) — trivially catchable with a
  one-line assertion (`tests/test_prices_daily_revisions.py` has one).
  Loud beats silent (see CLAUDE.md's SILENT SUCCESS standing instruction
  — this decision is instance #3 of the four listed there).
- **`daily.py` writes a new `prices_daily` row ONLY on an actual value
  change**, never unconditionally. The rolling 7-day window re-touches
  the same days every run; blind appending would add ~7 days × ~962
  tickers × 2 sources ≈ 13k rows/day that record no new information.
- **2020-01-02 is the honest modeling window, not ~2010 as originally
  hoped.** IDX's `GetStockSummary` endpoint has no data before that date
  — verified empirically (every 2010-2019 date probed returns
  `recordsTotal: 0` on a clean 200 OK), not a bug or a rate limit.
  Consequence: delisted tickers are only discoverable from 2020 onward.
  Pre-2020 Yahoo history's status was further downgraded on 2026-08-09 —
  see "CONSEQUENCE" above — from exploration-only to not usable at all,
  once split-contamination and silent freezes turned out to be
  undetectable there (no second source to check against).
- **IDX is authoritative for `close_raw`, Yahoo for `close_adj` (decided
  2026-08-09).** Not a per-case judgment call — a standing policy, because
  the failure modes behind it (retroactive split adjustment, multi-day
  outages, silent freezes) are systemic to Yahoo's raw-price feed, not
  occasional. See THE BIG FINDING above for the evidence.
- **Suspensions must NOT go into `known_issues`.** That table is for
  permanent, understood defects (spec-external facts that won't change:
  the 2007 Yahoo feed anomaly, a ticker's structurally sparse history).
  A suspension is temporary and ends; suppressing it permanently would
  hide a real re-listing. This is why remaining thread 3 proposes a
  separate `ticker_status_daily` concept instead.
- **Survivorship bias is documented, not laundered.** `securities` is
  962 active + 27 delisted (Phase 1b), explicitly biased for anything
  delisted before 2020-01-01. The README and this file say so in plain
  terms.
- **The corporate-actions review file is tiered by verification strength
  with per-row confidence, not a flat list.** Nothing gets applied to
  `corporate_actions` without human sign-off, tier by tier — a wrong
  corporate action is worse than a missing one (it would silently corrupt
  the adjustment basis for every downstream reader).

## Gotchas

- **Postgres `ON CONFLICT DO NOTHING` still performs a speculative
  insertion internally, even on a true no-op conflict.** Cost `securities`
  (989 rows) 84MB of dead-tuple bloat from ~1.6M redundant harvester
  calls before it was caught and fixed with a known-tickers cache. Regular
  `VACUUM`/autovacuum reclaims dead-tuple space for *reuse*, it does
  **not** shrink the file — only `VACUUM FULL` does that, and it takes an
  exclusive lock. Watch for this pattern anywhere a "does this already
  exist, insert if not" check runs at high frequency against a
  mostly-already-populated table.
- **A "frozen run" detector needs its threshold applied to the FULL
  window, not just past a noise floor.** A short coincidental repeat on
  the side that's actually moving normally (e.g. two days landing on the
  same price by chance) can clear the same low length threshold the real
  long freeze clears on the other side, causing a false "both sides
  frozen" classification that hides the real, single-sided defect. Found
  via `ADMF` (would have been misfiled into the zero-volume/suspension
  bucket, invisible). Locked in with a regression test in
  `tests/test_classify_discrepancies.py`.
- **A ratio-based anomaly detector needs a plausibility bound on the
  ratio itself, not just a near-zero-denominator guard.** `BCIC`: Yahoo
  carried a literal 5,000,000 close for 6 days — a corrupted value, not a
  price — with a completely normal (non-near-zero) IDX-side denominator,
  so a denominator guard alone does not catch it. Separately, `DSSA`
  showed why you also need to verify your own methodology's core
  assumption holds per-case: the detector assumed "ratio converges back
  to ~1.0 after a real transition," but DSSA's flagged transition never
  actually converged — it was a partial artifact of a much larger,
  pre-existing mismatch, not a resolved split. Both are now excluded with
  logged reasons in the review file rather than silently dropped or
  wrongly included.
- **Railway cron runs in UTC.** Relevant whenever Railway actually gets
  provisioned (thread 6) — spec's "18:30 WIB (11:30 UTC)" schedule needs
  the UTC figure there, not the WIB one. (launchd, by contrast, uses the
  machine's local system timezone — see the caveat baked into
  `launchd/com.idx.daily.plist` itself.)
- **launchd, not cron, on macOS** — specifically because
  `StartCalendarInterval` runs a missed job when the machine wakes from
  sleep, which plain cron does not. A laptop that's asleep at 18:30 WIB
  every day would silently never run under cron.
